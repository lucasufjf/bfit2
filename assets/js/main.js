import './controller.js';

document.addEventListener('DOMContentLoaded', () => {
    // Additional initialization code can go here if needed
    console.log("Main JS is loaded and DOM is fully loaded.");
});
